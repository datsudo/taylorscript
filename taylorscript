#!/usr/bin/env sh

SCRIPT="$1"


if [[ !(-z "$SCRIPT") ]]; then
    java -classpath target/classes com.taylorscript.main.TaylorScript "$SCRIPT"
    exit 0
fi

java -classpath target/classes com.taylorscript.main.TaylorScript
